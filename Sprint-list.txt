Sp1:
- Map all URL to FrontServlet which print the URL (a default page of some sort, like an error if the page doesn't exist)
- But before using the FrontServlet
    -> check if ressource exists in the FrontServlet (using custom method or a filter), 
        if yes then use requestDispatcher to that ressource, 
    -> use a filter/a custom class, filter associate to /* first instead of FrontServlet, then the filter would look for the ressource, 
        if not exist => use FS

Sp2:
- We have to know all URLs that are inside the project itself (uses of annotation)
    => create an annotation (for methods) which takes a parameter (an URL)
    => test if the annotation works (not in the framework, but in the test???), create a class with void main

    => when doing an url that isn't inside our annotation, return to error404 
    => need to know all the URLs annotation within all our classes
        => use hashmap<url,hashmap<>>


Sprint 2.1:
    annotation sur classe: create AnotController
    In an isolated project:
    - test1.java: no controller
    - test2.java: w/ controller
    - test3.java: w/ controller
    do a main.java then execute -> scan everthing in classpath (or give list of package where there are controller)
                                -> does the file have 

Sprint 3:
    at starting up/first call of servelt (do it in init of the fronservlet) or even better before anything has access to the app
    a code will be executed only once for performance reason, that code scans all classes in the classpath:
    "which one are annoted controller"
    return a list and store it while the app is alive.

    goal: "here are the urls supported"
    "here are the class and method associated to these URL (store those)

    if supported: show this is the class, this is the method
    else error 404

Sprint 4:
    Invoke a method associated to the url 

    Controller:
        test if return val is varchar
        Public String method {return ...}
            if returned varchar: show on the page via a printwriter

Sprint 4 bis:
    Create in fmk a class 'modelView'
    Attribut: String view
    Method in controller: 
        new method that return modelView
        associate an URL to it
        can create one instance of modelView
        inside do mv.setView(test.jsp)

        getView put in the requestDispatcher

    Create test.jsp
    Test if calling associated url in the controller shows/render the test.jsp

    In test check return value 
